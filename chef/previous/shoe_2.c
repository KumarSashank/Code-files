#include <stdio.h>
#include <stdlib.h>

int main()
{
    int t, i, j;
    scanf("%d", &t);
    int a[t][3];
    for (i = 0; i < t; i++)
    {
        for (j = 0; j < 3; j++)
        {
            scanf("%d", &a[i][j]);
        }
    }
    for (i = 0; i < t; i++)
    {
        if (a[1][0] == 0 && a[])
        {
        }
        else
        {
            printf("0\n");
        }
    }
}